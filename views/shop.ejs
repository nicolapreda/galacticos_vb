<!DOCTYPE html>
<html lang="it">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shop | Galacticos Vele Blu</title>
    <link rel="stylesheet" href="/assets/style.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Anton&family=Inter:wght@400;700;900&display=swap"
        rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', sans-serif;
            background-color: #F3F4F6;
        }

        h1,
        h2,
        h3 {
            font-family: 'Anton', sans-serif;
            text-transform: uppercase;
        }
    </style>
</head>

<body class="text-galacticos-dark">
    <%- include('partials/navbar') %>

        <!-- Page Header -->
        <header class="bg-galacticos-dark text-white pt-32 pb-12 px-6">
            <div class="max-w-7xl mx-auto">
                <h1 class="text-6xl md:text-8xl font-black mb-4">Official Shop</h1>
                <p class="text-xl text-gray-300 max-w-2xl">Indossa i colori della tua squadra. Merchandising ufficiale
                    stagione 2025/2026.</p>
            </div>
        </header>

        <!-- Filter/Sort Bar -->
        <div class="bg-white border-b border-gray-200 sticky top-20 z-40">
            <div class="max-w-7xl mx-auto px-6 py-4 flex justify-between items-center">
                <span class="font-bold text-sm uppercase tracking-wider">
                    <%= products ? products.length : 0 %> Prodotti
                </span>
                <div class="flex gap-4">
                    <button class="text-sm font-bold uppercase hover:text-galacticos-blue">Filtra</button>
                    <button class="text-sm font-bold uppercase hover:text-galacticos-blue">Ordina</button>
                </div>
            </div>
        </div>

        <!-- Product Grid -->
        <main class="max-w-7xl mx-auto px-6 py-12">
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
                <% if (products && products.length> 0) { %>
                    <% products.forEach(product=> { %>
                        <div class="bg-white group cursor-pointer hover:shadow-xl transition-shadow duration-300">
                            <div class="aspect-square overflow-hidden bg-gray-100 relative">
                                <img src="<%= product.image || '/assets/shop-teaser.png' %>" alt="<%= product.name %>"
                                    class="w-full h-full object-cover object-center group-hover:scale-110 transition-transform duration-500">
                                <% if(product.stock < 5 && product.stock> 0) { %>
                                    <span
                                        class="absolute top-2 right-2 bg-red-600 text-white text-xs font-bold px-2 py-1 uppercase">Pochi
                                        pezzi</span>
                                    <% } else if (product.stock==0) { %>
                                        <span
                                            class="absolute top-0 left-0 w-full h-full bg-black/50 flex items-center justify-center text-white font-black text-xl uppercase">Sold
                                            Out</span>
                                        <% } %>
                            </div>
                            <div class="p-6">
                                <h3 class="text-xl font-bold mb-1">
                                    <%= product.name %>
                                </h3>
                                <p class="text-sm text-gray-500 mb-4 line-clamp-2">
                                    <%= product.description %>
                                </p>
                                <div class="flex justify-between items-center">
                                    <span class="text-lg font-black">€ <%= product.price.toFixed(2) %></span>
                                    <button
                                        class="bg-galacticos-dark text-white p-2 hover:bg-galacticos-blue transition-colors">
                                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                                d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z"></path>
                                        </svg>
                                    </button>
                                </div>
                            </div>
                        </div>
                        <% }); %>
                            <% } else { %>
                                <!-- Empty State -->
                                <div class="col-span-full text-center py-20">
                                    <h3 class="text-2xl font-bold text-gray-400 mb-4">Nessun prodotto disponibile</h3>
                                    <p class="text-gray-500">Torna a trovarci presto per i nuovi arrivi!</p>
                                </div>
                                <% } %>

                                    <!-- Static Example Item if empty (for demo) -->
                                    <% if (!products || products.length===0) { %>
                                        <div
                                            class="bg-white group cursor-pointer hover:shadow-xl transition-shadow duration-300 relative">
                                            <div class="aspect-square overflow-hidden bg-gray-100">
                                                <img src="/assets/shop-teaser.png" alt="Maglia Gara Home"
                                                    class="w-full h-full object-cover object-center group-hover:scale-110 transition-transform duration-500">
                                            </div>
                                            <div class="p-6">
                                                <h3 class="text-xl font-bold mb-1">Maglia Home 25/26</h3>
                                                <p class="text-sm text-gray-500 mb-4">La maglia ufficiale delle partite
                                                    in casa.</p>
                                                <div class="flex justify-between items-center">
                                                    <span class="text-lg font-black">€ 65.00</span>
                                                    <button
                                                        class="bg-galacticos-dark text-white p-2 hover:bg-galacticos-blue transition-colors">
                                                        <svg class="w-5 h-5" fill="none" stroke="currentColor"
                                                            viewBox="0 0 24 24">
                                                            <path stroke-linecap="round" stroke-linejoin="round"
                                                                stroke-width="2"
                                                                d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z"></path>
                                                        </svg>
                                                    </button>
                                                </div>
                                            </div>
                                        </div>
                                        <% } %>
            </div>
        </main>

        <%- include('partials/footer') %>
</body>

</html>